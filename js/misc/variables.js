//Global variables.

let $arena,
    playableWidth,
    playableHeight,
    platforms,
    keypress,
    keyArray,

    $ball,
    ballTop,
    ballLeft,
    ballRight,
    ballBottom,

    playerOne,
    playerTwo,

    laserInterval;
